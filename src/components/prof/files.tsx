import { FC } from "react";
import { ProfViewComponentProps } from "./util";

type FilesProfViewProps = ProfViewComponentProps & {}
const FilesProfView: FC<FilesProfViewProps> = () => {
    return (
        <div></div>
    );
}

export default FilesProfView;