import { NextPage } from 'next';

interface Props {
}
const ProfCardViewPage: NextPage<Props> = ({ }) => {
    return (
        <>
            prof card view page
        </>
    );
}
export default ProfCardViewPage;
